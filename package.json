{
  "name": "kumii-collaboration",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "build:api": "cd apps/api && npm run build",
    "build:web": "cd apps/web && npm run build",
    "build": "npm run build:api",
    "vercel-build": "npm install --workspaces && npm run build:web && npm run build:api",
    "install:all": "npm install && cd apps/api && npm install && cd ../web && npm install",
    "lint": "cd apps/api && npm run lint && cd ../web && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
