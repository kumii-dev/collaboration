# 📊 System Diagrams - Kumii Collaboration Module

Visual representations of system architecture, data flow, and component interactions.

---

## 🏛️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           USER DEVICES                               │
│         Web Browsers (Chrome, Safari, Firefox, Edge)                │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ HTTPS
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                      VERCEL EDGE NETWORK                             │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Frontend (React App)                       │  │
│  │  • React Router (SPA)                                        │  │
│  │  • Bootstrap UI                                              │  │
│  │  • React Query (State Management)                           │  │
│  │  • Supabase Client (Auth & Realtime)                        │  │
│  └──────────────────────────┬───────────────────────────────────┘  │
│                              │                                       │
│  ┌──────────────────────────▼───────────────────────────────────┐  │
│  │              Backend API (Express Serverless)                 │  │
│  │  • Authentication Middleware                                  │  │
│  │  • Rate Limiting & Security                                  │  │
│  │  • Business Logic & Validation                               │  │
│  │  • Email Service (Resend)                                    │  │
│  └──────────────────────────┬───────────────────────────────────┘  │
└─────────────────────────────┼───────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐   ┌──────────────────┐   ┌─────────────┐
│   Supabase    │   │   Supabase       │   │   Resend    │
│   Database    │   │   Storage        │   │   Email     │
│  (PostgreSQL) │   │  (File Storage)  │   │   Service   │
└───────────────┘   └──────────────────┘   └─────────────┘
```

---

## 🔄 Request Flow - Chat Message

```
┌─────┐                                                         ┌─────┐
│User │                                                         │User │
│  A  │                                                         │  B  │
└──┬──┘                                                         └──┬──┘
   │                                                               │
   │ 1. Send Message                                               │
   │ POST /api/chat/conversations/:id/messages                     │
   ├──────────────────────────────────────────────┐               │
   │                                               │               │
   │                                  ┌────────────▼───────────┐   │
   │                                  │   Backend API           │   │
   │                                  │                         │   │
   │                                  │  2. Validate Auth Token │   │
   │                                  │  3. Check Participant   │   │
   │                                  │  4. Sanitize Content    │   │
   │                                  │  5. Extract Mentions    │   │
   │                                  └────────────┬───────────┘   │
   │                                               │               │
   │                                               │ 6. Insert     │
   │                                               ▼               │
   │                                  ┌───────────────────────┐   │
   │                                  │   Supabase Database   │   │
   │                                  │                       │   │
   │                                  │  messages table       │   │
   │                                  │  ├─ Trigger fires     │   │
   │                                  │  └─ RLS checks       │   │
   │                                  └───────────┬───────────┘   │
   │                                               │               │
   │ 7. Response                                   │ 8. Realtime   │
   │ 201 Created                                   │    Broadcast  │
   │◄──────────────────────────────────────────────┤               │
   │                                               │               │
   │                                               └───────────────┤
   │                                                               │
   │                                          9. WebSocket Event   │
   │                                            New Message        │
   │                                               ┌───────────────▼──┐
   │                                               │ User B Frontend  │
   │                                               │ Updates UI       │
   │                                               └──────────────────┘
   │
   │ 10. Send Email (async)
   │ ┌────────────────┐
   └─┤ Resend Service ├──> 📧 Email to mentioned users
     └────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │
     │ 1. Enter email + password
     │
     ▼
┌─────────────────┐
│ Login Page      │
│ (Frontend)      │
└────┬────────────┘
     │
     │ 2. signInWithPassword()
     │
     ▼
┌─────────────────────────────────┐
│   Supabase Auth                 │
│                                 │
│  ┌──────────────────────────┐  │
│  │ 1. Verify credentials    │  │
│  │ 2. Check email confirmed │  │
│  │ 3. Generate JWT          │  │
│  └──────────────────────────┘  │
└────┬────────────────────────────┘
     │
     │ 3. Return { session, user }
     │
     ▼
┌─────────────────┐
│ Frontend        │
│ Store JWT token │
└────┬────────────┘
     │
     │ 4. Fetch user profile
     │    GET /api/profiles/me
     │    Authorization: Bearer <JWT>
     │
     ▼
┌─────────────────────────────┐
│   Backend API               │
│                             │
│  ┌──────────────────────┐  │
│  │ authenticate()       │  │
│  │  ├─ Extract token    │  │
│  │  ├─ Verify with      │  │
│  │  │   Supabase        │  │
│  │  └─ Get user role    │  │
│  └──────────────────────┘  │
└────┬────────────────────────┘
     │
     │ 5. Query database
     │    SELECT * FROM profiles WHERE id = auth.uid()
     │
     ▼
┌─────────────────────┐
│ PostgreSQL + RLS    │
│                     │
│  RLS Policy Check:  │
│  • Is user authed?  │
│  • Matches auth.uid?│
└────┬────────────────┘
     │
     │ 6. Return profile with role
     │
     ▼
┌─────────────────┐
│ Frontend        │
│ • Store user    │
│ • Store role    │
│ • Redirect home │
└─────────────────┘
```

---

## 🗄️ Database Schema (Simplified)

```
┌──────────────┐
│   profiles   │
│──────────────│
│ id (PK)      │───┐
│ email        │   │
│ role         │   │  Creator
│ reputation   │   │
└──────────────┘   │
                   │
                   │
       ┌───────────┴──────────┬────────────────┐
       │                      │                │
       ▼                      ▼                ▼
┌──────────────┐      ┌──────────────┐  ┌──────────────┐
│conversations │      │forum_threads │  │   messages   │
│──────────────│      │──────────────│  │──────────────│
│ id (PK)      │──┐   │ id (PK)      │  │ id (PK)      │
│ type         │  │   │ title        │  │ content      │
│ created_by   │──┘   │ author_id    │──┘ │ sender_id   │──┘
└──────────────┘      └──────┬───────┘  └──────┬───────┘
       │                     │                 │
       │ Participants        │ Posts           │ Reactions
       │                     │                 │
       ▼                     ▼                 ▼
┌─────────────────────┐  ┌──────────────┐  ┌──────────────────┐
│conversation_        │  │ forum_posts  │  │message_reactions │
│  participants       │  │──────────────│  │──────────────────│
│─────────────────────│  │ id (PK)      │  │ id (PK)          │
│ conversation_id (FK)│  │ thread_id(FK)│  │ message_id (FK)  │
│ user_id (FK)        │  │ author_id(FK)│  │ user_id (FK)     │
│ joined_at           │  │ content      │  │ emoji            │
└─────────────────────┘  └──────────────┘  └──────────────────┘

                    ┌──────────────┐
                    │forum_boards  │
                    │──────────────│
                    │ id (PK)      │
                    │ category_id  │
                    │ name         │
                    │ access_role  │
                    └──────┬───────┘
                           │
                           │
                    ┌──────▼────────┐
                    │forum_categories│
                    │───────────────│
                    │ id (PK)       │
                    │ name          │
                    │ description   │
                    └───────────────┘
```

---

## 🔒 Row Level Security (RLS) Model

```
┌─────────────────────────────────────────────────────────┐
│              PostgreSQL Database                         │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  All queries run with user context:            │    │
│  │  SET LOCAL role = 'authenticated';             │    │
│  │  SET LOCAL request.jwt.claim.sub = '<user_id>';│    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  User makes query: SELECT * FROM messages;              │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  RLS Policy Evaluation (BEFORE query runs)     │    │
│  │                                                 │    │
│  │  1. Check: Is user authenticated?              │    │
│  │     ├─ YES → Continue                           │    │
│  │     └─ NO → Return empty set                    │    │
│  │                                                 │    │
│  │  2. Check: Is user a conversation participant? │    │
│  │     SELECT 1 FROM conversation_participants    │    │
│  │     WHERE user_id = auth.uid()                  │    │
│  │     AND conversation_id = messages.conv_id      │    │
│  │     ├─ YES → Allow row                          │    │
│  │     └─ NO → Filter out row                      │    │
│  │                                                 │    │
│  │  3. Check: Is message deleted?                 │    │
│  │     ├─ YES → Filter out                         │    │
│  │     └─ NO → Allow                               │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  Query returns: Only messages user can access           │
│                                                          │
└─────────────────────────────────────────────────────────┘

Role Hierarchy:
┌─────────────┐
│    admin    │ ← Full access to everything
└──────┬──────┘
       │
┌──────▼──────┐
│  moderator  │ ← Can moderate content + reports
└──────┬──────┘
       │
┌──────▼──────┬──────────────┐
│   advisor   │    funder    │ ← Access to special boards
└──────┬──────┴──────┬───────┘
       │             │
┌──────▼─────────────▼───────┐
│      entrepreneur          │ ← Default user role
└────────────────────────────┘
```

---

## 📨 Notification System Flow

```
                    Triggering Event
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
   New Reply        @Mention          Moderation
                                        Action
        │                 │                 │
        └─────────────────┴─────────────────┘
                          │
                          ▼
              ┌───────────────────────┐
              │  Backend API          │
              │  (Non-blocking async) │
              └───────────┬───────────┘
                          │
              ┌───────────┴───────────┐
              │                       │
              ▼                       ▼
    ┌─────────────────┐     ┌────────────────┐
    │   Database      │     │  Email Service │
    │                 │     │    (Resend)    │
    │ INSERT INTO     │     │                │
    │ notifications   │     │ ┌────────────┐ │
    │                 │     │ │ Retry #1   │ │
    │ ├─ user_id      │     │ └─────┬──────┘ │
    │ ├─ type         │     │       │        │
    │ ├─ content      │     │    Fail?       │
    │ └─ read=false   │     │       │        │
    └────────┬────────┘     │ ┌─────▼──────┐ │
             │              │ │ Retry #2   │ │
             │              │ │ (+2s delay)│ │
             │              │ └─────┬──────┘ │
             │              │       │        │
             │              │    Fail?       │
             │              │       │        │
             │              │ ┌─────▼──────┐ │
             │              │ │ Retry #3   │ │
             │              │ │ (+4s delay)│ │
             │              │ └─────┬──────┘ │
             │              │       │        │
             │              │  Success/Fail  │
             │              │       │        │
             │              │ ┌─────▼──────┐ │
             │              │ │ Log Result │ │
             │              │ └────────────┘ │
             │              └────────────────┘
             │
             │ Realtime broadcast
             │
             ▼
    ┌─────────────────┐
    │ User's Browser  │
    │                 │
    │ WebSocket       │
    │ receives update │
    │                 │
    │ Shows 🔴 badge  │
    │ in navbar       │
    └─────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    PRODUCTION SETUP                         │
└────────────────────────────────────────────────────────────┘

                        User Request
                             │
                             ▼
                   ┌──────────────────┐
                   │   DNS (Route53)  │
                   │  kumii.co.za     │
                   └────────┬─────────┘
                            │
              ┌─────────────┴─────────────┐
              │                           │
              ▼                           ▼
    ┌──────────────────┐        ┌──────────────────┐
    │  app.kumii.co.za │        │  api.kumii.co.za │
    │                  │        │                  │
    │  Vercel CDN      │        │  Vercel Edge     │
    │  (Static Files)  │        │  (Serverless)    │
    │                  │        │                  │
    │  ┌────────────┐  │        │  ┌────────────┐  │
    │  │  React SPA │  │        │  │  Express   │  │
    │  │  (Built)   │  │        │  │  API       │  │
    │  └────────────┘  │        │  └────────────┘  │
    └──────────────────┘        └────────┬─────────┘
                                         │
                                         │
              ┌──────────────────────────┴──────┐
              │                                 │
              ▼                                 ▼
    ┌──────────────────┐              ┌─────────────────┐
    │  Supabase        │              │   Resend        │
    │                  │              │                 │
    │  • PostgreSQL    │              │  • Email SMTP   │
    │  • Auth          │              │  • Templates    │
    │  • Realtime      │              │  • Analytics    │
    │  • Storage       │              │                 │
    │                  │              │                 │
    │  Region: US-EAST │              │  Region: Global │
    └──────────────────┘              └─────────────────┘

Deployment Flow:
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│ Developer   │       │   GitHub    │       │   Vercel    │
│ git push    │──────▶│   Webhook   │──────▶│   Build     │
└─────────────┘       └─────────────┘       └──────┬──────┘
                                                    │
                                              Automatic:
                                              • npm install
                                              • npm run build
                                              • Deploy to CDN
                                              • Health check
                                                    │
                                                    ▼
                                            ┌───────────────┐
                                            │   LIVE ✅     │
                                            └───────────────┘
```

---

## 🔄 Data Synchronization (Realtime)

```
┌────────────────────────────────────────────────────────────┐
│              Supabase Realtime Architecture                 │
└────────────────────────────────────────────────────────────┘

                    PostgreSQL Database
                            │
                    WAL (Write-Ahead Log)
                            │
                            ▼
                ┌───────────────────────┐
                │  Supabase Realtime    │
                │  (Phoenix Framework)  │
                └───────────┬───────────┘
                            │
              Broadcast to subscribed clients
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
    User A              User B              User C
    (Online)            (Online)            (Offline)
        │                   │                   
   ┌────▼────┐         ┌────▼────┐         
   │ Update  │         │ Update  │         
   │ UI      │         │ UI      │         
   │ Live    │         │ Live    │         
   └─────────┘         └─────────┘         

Frontend Subscription Code:
┌────────────────────────────────────────────┐
│ const channel = supabase                   │
│   .channel('conversation:123')             │
│   .on('postgres_changes', {                │
│     event: 'INSERT',                       │
│     schema: 'public',                      │
│     table: 'messages',                     │
│     filter: 'conversation_id=eq.123'       │
│   }, (payload) => {                        │
│     // Add new message to UI               │
│     setMessages(prev => [...prev, payload])│
│   })                                        │
│   .subscribe();                            │
└────────────────────────────────────────────┘

Events Broadcast:
• New messages (chat)
• New posts (forum)
• Typing indicators
• Read receipts
• Reactions
• User presence
```

---

## 📊 Component Hierarchy (Frontend)

```
App.tsx
│
├─ Router
│  │
│  ├─ LoginPage (public)
│  │
│  └─ MainLayout (authenticated)
│     │
│     ├─ Sidebar
│     │  ├─ Logo
│     │  ├─ Navigation Links
│     │  │  ├─ Dashboard
│     │  │  ├─ Chat
│     │  │  ├─ Forums
│     │  │  ├─ Moderation (if moderator)
│     │  │  └─ Settings
│     │  └─ User Section
│     │
│     ├─ Topbar
│     │  ├─ Search Bar
│     │  ├─ Notifications Badge
│     │  └─ Profile Dropdown
│     │
│     └─ Main Content Area
│        │
│        ├─ ChatPage
│        │  ├─ ConversationList
│        │  │  └─ ConversationItem (multiple)
│        │  ├─ MessageThread
│        │  │  └─ MessageBubble (multiple)
│        │  └─ MessageComposer
│        │     ├─ TextArea
│        │     ├─ AttachButton
│        │     └─ SendButton
│        │
│        ├─ ForumPage
│        │  ├─ CategoryList
│        │  │  └─ CategoryCard (multiple)
│        │  ├─ ThreadList
│        │  │  └─ ThreadItem (multiple)
│        │  └─ ThreadView
│        │     ├─ ThreadHeader
│        │     ├─ PostList
│        │     │  └─ PostCard (multiple)
│        │     └─ ReplyComposer
│        │
│        ├─ ModerationPage
│        │  ├─ ReportsQueue
│        │  │  └─ ReportCard (multiple)
│        │  ├─ ModerationActionForm
│        │  └─ AuditLogViewer
│        │
│        └─ ProfilePage
│           ├─ ProfileHeader
│           ├─ ProfileStats
│           ├─ RecentActivity
│           └─ EditProfileForm
│
└─ Global Providers
   ├─ QueryClientProvider (React Query)
   ├─ AuthContext
   └─ ThemeContext
```

---

## 🧪 Testing Strategy Diagram

```
┌────────────────────────────────────────────────────────────┐
│                     Testing Pyramid                         │
└────────────────────────────────────────────────────────────┘

                        ▲
                       ╱ ╲
                      ╱   ╲
                     ╱ E2E ╲           • Full user flows
                    ╱ Tests ╲          • Playwright/Cypress
                   ╱─────────╲         • 10-20 critical paths
                  ╱           ╲
                 ╱             ╲
                ╱  Integration  ╲      • API endpoints
               ╱     Tests       ╲     • Database operations
              ╱───────────────────╲    • RLS policies
             ╱                     ╲   • 50-100 tests
            ╱                       ╲
           ╱         Unit           ╲  • Functions
          ╱          Tests           ╲ • Components
         ╱─────────────────────────── ╲• Utilities
        ╱                             ╲• 200-500 tests
       ╱───────────────────────────────╲
      └─────────────────────────────────┘

Test Coverage Goals:
┌──────────────────┬─────────┐
│ Unit Tests       │  > 80%  │
│ Integration Tests│  > 60%  │
│ E2E Tests        │  > 40%  │
└──────────────────┴─────────┘
```

---

**Last Updated:** January 27, 2026  
**Version:** 1.0.0
